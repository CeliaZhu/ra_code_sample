# Use p_load function from the pacman package to automatically check for and
# install the package that's not on the current system.

p_load(here, estimatr, tidyverse, stringr, testthat, lubridate, gtsummary)
